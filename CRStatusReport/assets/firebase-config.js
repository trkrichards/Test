// Fill these in with your own Firebase project's values.
// Firebase Console → Project settings → General → "Your apps" → SDK setup and
// configuration → Config. These values are not secret (they're safe to ship
// in client-side code) — access control is handled by Firestore Security
// Rules, not by hiding this file.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
